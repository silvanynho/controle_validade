name: nutri_control
description: Nutri Control - Controle inteligente de estoque e validade
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.1
  cloud_firestore: ^4.13.6
  mobile_scanner: ^5.1.1 # NOVO LEITOR, MAIS COMPATÍVEL
  intl: ^0.19.0

flutter:
  uses-material-design: true
