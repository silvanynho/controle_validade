name: controle_validade
description: Aplicativo de Controle de Estoque e Validade
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^5.1.1
  intl: ^0.19.0
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.5
  firebase_auth: ^4.20.0
  flutter_local_notifications: ^17.2.2
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
  
