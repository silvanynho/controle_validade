workflows:
  android-apk:
    name: Gerar APK Atualizado
    environment:
      flutter: 3.22.0
    scripts:
      - flutter pub get
      - flutter create --platforms=android --overwrite .
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
