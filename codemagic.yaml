workflows:
  android-apk:
    name: Nutri Control

    environment:
      flutter: 3.16.9

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/*.apk

    cache:
      cache_paths:
        - ~/.pub-cache
